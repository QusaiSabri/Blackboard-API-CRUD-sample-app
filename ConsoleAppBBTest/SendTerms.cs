using System;

namespace ConsoleAppBBTest
{
    public static class SendTerms
    {
        internal static void Run()
        {
            Console.WriteLine("Running Terms...");

            Console.WriteLine("Get my Terms from my database...");

            //Get terms from my data source

            //Loop through all terms and do the following for each term

            //Are we Creating, Updating or Deleting this term from BB?

            //If Creating a new Term?
            //var processRequest = new ProcessRequest();
            //processRequest.doTerm(Operations.Create, term)


            //If Updating a Term?
            //var processRequest = new ProcessRequest();
            //processRequest.doTerm(Operations.Update, term)


            //If Deleting a Term?
            //var processRequest = new ProcessRequest();
            //processRequest.doTerm(Operations.Delete, term)


        }
    }
}
